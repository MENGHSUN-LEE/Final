const config = {
    db: {
        host: "final-mysql", 
        user: "root", 
        password: "417",
        database: "final_db",
        connectTimeout: 60000 
    },
};

module.exports = config;