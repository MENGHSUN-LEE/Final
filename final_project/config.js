const config = {
    db: {
        host: "final-mysql", 
        user: "root", 
        password: "YourSecureRootPassword", 
        database: "final_project_db", 
        connectTimeout: 60000 
    },
};

module.exports = config;